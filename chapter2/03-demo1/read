# 今回のコマンド

```jsx
window.alert('はじめてのJavascript！');
```

## オブジェクト

**window. alert('はじめてのJavascript！');**

- オブジェクトとは**「これから動作させる対象となるもの」**を指示する
    
    今回は「window」としているのでブラウザのウィンドウ事態を指す
    
    - **window**      ：ウィンドウ
    - **history**       ：履歴
    - **location**     ：アドレス情報
    - **document** ：HTML全体
    - **console**　  ：コンソール

## メソッドとパラメータ

**window. alert('はじめてのJavascript！');**

- メソッドとは**「動作・命令」**を指示する
    
    今回の「alert」は「警報」という意味で、Javascriptでは「警告ダイアログを表示する」という動きになる。
    

- パラメータ（調整値）とは**「メソッドを補足する情報」**を指示する
    
    今回は「alert()」の()内に警告ダイアログの警告内容を記述することで
    
    パラメーターに指定したメッセージが表示される